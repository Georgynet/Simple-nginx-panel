{%  if (form is not null) %}
<h1>Новый сайт</h1>
<form action="{{ url('rest_post_site') }}" method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" value="submit"/>
</form>
{%  endif %}